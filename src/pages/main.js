import React from 'react';
import { Layout } from '../components/common/layout';

export const MainPage = () => (
  <Layout pageTitle="Main page">
    <div>Hello, welcome to out awesome store</div>
  </Layout>
);
